#!/bin/bash

check() {
    updates=$(cat check.txt | wc -l)
    echo "$updates updates "
}

if [ -f check.txt ]
then
    check
else
    checkupdates > check.txt
    check
    sleep 5
    rm check.txt
fi
